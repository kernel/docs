{
  "$source": "kernel/kernel packages/api/lib/compat/network_surface.json. Copies in kernel/infra and kernel/docs are vendored and must stay byte-identical.",
  "destinations": [
    {
      "feature": "API, SDKs, and authenticated CLI requests",
      "hosts": ["api.onkernel.com"],
      "port": 443,
      "protocol": "HTTPS"
    },
    {
      "feature": "CLI authentication (`kernel login`)",
      "hosts": ["auth.onkernel.com", "dashboard.onkernel.com"],
      "port": 443,
      "protocol": "HTTPS"
    },
    {
      "feature": "CDP, WebDriver BiDi, Live View, replays, and app connections",
      "hosts": ["*.onkernel.com", "*.kernel.sh"],
      "port": 8443,
      "protocol": "HTTPS and secure WebSocket (WSS)",
      "session_facing": true
    },
    {
      "feature": "Live View media negotiation",
      "hosts": ["stun.relay.metered.ca"],
      "port": 80,
      "protocol": "STUN over UDP"
    },
    {
      "feature": "Live View media relay",
      "hosts": ["global.relay.metered.ca"],
      "port": 443,
      "protocol": "TURN over UDP or TLS/TCP"
    },
    {
      "feature": "Replay files",
      "hosts": ["kernel-api-prod.s3.us-east-1.amazonaws.com"],
      "port": 443,
      "protocol": "HTTPS"
    }
  ],
  "not_covered": [
    "The specific hostname inside a wildcard, as returned in `cdp_ws_url`, `webdriver_ws_url` or `browser_live_view_url`. It changes between sessions.",
    "Which metro or host serves a given session.",
    "Egress IP addresses, including residential, ISP and datacenter proxy exit IPs."
  ]
}
